export const metadata = {
  title: "Central Directorate Administrator Dashboard | DigiPass Legal Metrology",
  description: "National metrology regulatory oversight, inspector allocation, and enforcement command center.",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
